---
import ProjectCard from '../components/ProjectCard.astro';
import projects from '../data/projects.json';
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>Abandoned Projects - Security Risks & Compatibility Issues</title>
    <meta name="description" content="A curated list of abandoned GitHub projects that pose security risks and package compatibility problems." />
  </head>
  <body>
    <div class="container">
      <header>
        <h1>⚠️ Abandoned Projects</h1>
        <p class="subtitle">
          A curated showcase of unmaintained GitHub projects that pose security risks and package compatibility issues
        </p>

        <div class="warning-box">
          <p>
            These projects are either officially deprecated, abandoned by maintainers, or no longer receiving security updates.
            Using them in production can lead to security vulnerabilities, compatibility problems, and maintenance headaches.
          </p>
        </div>

        <div class="about-section">
          <h2>🎯 Purpose</h2>
          <p>
            This project highlights popular open-source packages that have been abandoned, deprecated, or left unmaintained.
            When developers continue to use these packages, they expose their projects to:
          </p>
          <ul>
            <li><strong>Security vulnerabilities</strong> - No patches for newly discovered exploits</li>
            <li><strong>Compatibility issues</strong> - Breaking changes with modern tooling and dependencies</li>
            <li><strong>Technical debt</strong> - Harder to maintain and migrate away from over time</li>
            <li><strong>Supply chain risks</strong> - Potential targets for malicious takeovers</li>
          </ul>

          <h2>🚀 Features</h2>
          <ul>
            <li><strong>Visual Card Layout</strong> - Easy-to-scan cards displaying project information</li>
            <li><strong>Last Commit Tracking</strong> - Shows how long each project has been abandoned</li>
            <li><strong>Risk Assessment</strong> - Explains why each abandoned project matters</li>
            <li><strong>GitHub Integration</strong> - Direct links to repositories with star counts</li>
            <li><strong>Responsive Design</strong> - Works seamlessly on mobile and desktop</li>
          </ul>

          <div class="contribute-box">
            <h3>🤝 Want to Contribute?</h3>
            <p>
              Know of an abandoned project that should be listed? Check out the
              <a href="https://github.com/GeorgePearse/AbandonedProjects#readme" target="_blank" rel="noopener noreferrer">
                README on GitHub
              </a> for contribution guidelines and add it to the list!
            </p>
          </div>
        </div>
      </header>

      <main>
        <h2 class="projects-title">📊 Featured Abandoned Projects</h2>
        <div class="projects-grid">
          {projects.map((project) => (
            <ProjectCard
              name={project.name}
              repo={project.repo}
              url={project.url}
              description={project.description}
              lastCommit={project.lastCommit}
              stars={project.stars}
              reason={project.reason}
            />
          ))}
        </div>
      </main>

      <footer>
        <p>
          Data last updated: {new Date().toLocaleDateString()}
        </p>
        <p>
          Built to raise awareness about abandoned dependencies in the JavaScript ecosystem.
        </p>
        <p>
          <a href="https://github.com/GeorgePearse/AbandonedProjects" target="_blank" rel="noopener noreferrer">
            View on GitHub
          </a>
          {' • '}
          <a href="https://github.com/GeorgePearse/AbandonedProjects#readme" target="_blank" rel="noopener noreferrer">
            Read the Docs
          </a>
          {' • '}
          <a href="https://github.com/GeorgePearse/AbandonedProjects/issues" target="_blank" rel="noopener noreferrer">
            Report an Issue
          </a>
        </p>
        <p>
          Built with <a href="https://claude.com/claude-code" target="_blank" rel="noopener noreferrer">Claude Code</a>
        </p>
      </footer>
    </div>
  </body>
</html>

<style>
  :root {
    --bg-primary: #0d1117;
    --bg-secondary: #161b22;
    --card-bg: #1c2128;
    --card-border: #30363d;
    --text-primary: #e6edf3;
    --text-secondary: #8b949e;
    --accent: #58a6ff;
    --danger: #f85149;
    --status-bg: #161b22;
    --warning-bg: #1f2937;
    --warning-border: #f59e0b;
  }

  @media (prefers-color-scheme: light) {
    :root {
      --bg-primary: #ffffff;
      --bg-secondary: #f6f8fa;
      --card-bg: #ffffff;
      --card-border: #d0d7de;
      --text-primary: #1f2328;
      --text-secondary: #57606a;
      --accent: #0969da;
      --danger: #cf222e;
      --status-bg: #f6f8fa;
      --warning-bg: #fff8e6;
      --warning-border: #f59e0b;
    }
  }

  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }

  body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Noto Sans', Helvetica, Arial, sans-serif;
    background: var(--bg-primary);
    color: var(--text-primary);
    line-height: 1.6;
    min-height: 100vh;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
  }

  header {
    text-align: center;
    margin-bottom: 3rem;
  }

  h1 {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 1rem;
    background: linear-gradient(135deg, var(--accent), var(--danger));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .subtitle {
    font-size: 1.25rem;
    color: var(--text-secondary);
    margin-bottom: 2rem;
  }

  .warning-box {
    background: var(--warning-bg);
    border: 2px solid var(--warning-border);
    border-radius: 12px;
    padding: 1.5rem;
    max-width: 800px;
    margin: 0 auto;
  }

  .warning-box p {
    color: var(--text-primary);
    line-height: 1.7;
  }

  .about-section {
    max-width: 900px;
    margin: 3rem auto 0;
    text-align: left;
    background: var(--card-bg);
    border: 1px solid var(--card-border);
    border-radius: 12px;
    padding: 2rem;
  }

  .about-section h2 {
    font-size: 1.5rem;
    margin: 2rem 0 1rem 0;
    color: var(--text-primary);
  }

  .about-section h2:first-of-type {
    margin-top: 0;
  }

  .about-section p {
    color: var(--text-secondary);
    line-height: 1.7;
    margin-bottom: 1rem;
  }

  .about-section ul {
    list-style: none;
    padding: 0;
    margin: 1rem 0;
  }

  .about-section li {
    color: var(--text-secondary);
    line-height: 1.8;
    padding: 0.5rem 0;
    padding-left: 1.5rem;
    position: relative;
  }

  .about-section li:before {
    content: "→";
    position: absolute;
    left: 0;
    color: var(--accent);
    font-weight: bold;
  }

  .contribute-box {
    background: var(--status-bg);
    border: 2px solid var(--accent);
    border-radius: 8px;
    padding: 1.5rem;
    margin-top: 2rem;
  }

  .contribute-box h3 {
    margin: 0 0 0.75rem 0;
    color: var(--accent);
    font-size: 1.25rem;
  }

  .contribute-box p {
    margin: 0;
    color: var(--text-primary);
  }

  .contribute-box a {
    color: var(--accent);
    text-decoration: underline;
    font-weight: 500;
  }

  .contribute-box a:hover {
    color: var(--text-primary);
  }

  .projects-title {
    text-align: center;
    font-size: 2rem;
    margin: 4rem 0 2rem 0;
    color: var(--text-primary);
  }

  .projects-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
    gap: 2rem;
    margin-bottom: 3rem;
  }

  footer {
    text-align: center;
    padding: 2rem 0;
    border-top: 1px solid var(--card-border);
    color: var(--text-secondary);
    font-size: 0.875rem;
  }

  footer p {
    margin: 0.5rem 0;
  }

  footer a {
    color: var(--accent);
    text-decoration: none;
    transition: color 0.2s ease;
  }

  footer a:hover {
    color: var(--text-primary);
    text-decoration: underline;
  }

  @media (max-width: 768px) {
    .container {
      padding: 1rem;
    }

    h1 {
      font-size: 2rem;
    }

    .subtitle {
      font-size: 1rem;
    }

    .projects-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }
  }
</style>
